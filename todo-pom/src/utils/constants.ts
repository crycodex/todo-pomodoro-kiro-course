/**
 * Application constants for todo-pom
 */

/** Duration of a Pomodoro work session in seconds (25 minutes) */
export const WORK_DURATION_SECONDS = 25 * 60

/** Duration of a short break in seconds (5 minutes) */
export const BREAK_DURATION_SECONDS = 5 * 60

/** Key used for localStorage persistence */
export const STORAGE_KEY = 'todo-pom-state'

/** Maximum allowed length for a task title */
export const MAX_TITLE_LENGTH = 200
